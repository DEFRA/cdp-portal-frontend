# Availability

## Behaviour

- Shows a loading spinner while checking
- Displays a tick icon when the name is available
- Displays a block icon when the name is unavailable
- Automatically triggers when the user types in the target input

## JavaScript

The component requires client-side JavaScript to function. It monitors the target input and makes requests to the `dataFetcher` endpoint.
