{% macro appProgress(params) %}
  <app-progress
    {% if params.id %}id={{ params.id }}{% endif %}
    data-progress="{{ params.progress }}"
    data-complete="{{ params.complete }}"
    data-total="{{ params.total }}"
  >
    {%- include "./template.njk" -%}
  </app-progress>
{% endmacro %}
