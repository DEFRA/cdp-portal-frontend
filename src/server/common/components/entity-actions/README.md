# Entity Actions

## Layout

The component creates a form with:

1. **Legend** - Optional page heading
2. **Primary actions** - Search input
3. **Filters** - Additional filter controls

## Notes

- Uses `data-js="auto-submit"` for automatic form submission on change
- Includes a hidden submit button for non-JavaScript users
- Filter components are typically `appSearch` and `appSelect` components
