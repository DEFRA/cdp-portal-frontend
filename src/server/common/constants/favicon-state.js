/**
 * @type {{pending: string, success: string, failed: string, stopped: string}}
 */
const faviconState = {
  pending: 'pending',
  success: 'success',
  failed: 'failed',
  stopped: 'stopped'
}

export { faviconState }
