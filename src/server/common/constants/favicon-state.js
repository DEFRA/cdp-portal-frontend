/**
 * @type {{pending: string, success: string, failed: string, stopped: string, undeployed: string}}
 */
const faviconState = {
  pending: 'pending',
  success: 'success',
  failed: 'failed',
  stopped: 'stopped',
  undeployed: 'undeployed'
}

export { faviconState }
