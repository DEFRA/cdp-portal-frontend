const taskStatus = {
  starting: 'starting',
  inProgress: 'in-progress',
  stopping: 'stopping',
  failed: 'failed',
  finished: 'finished'
}

const testStatus = {
  passed: 'passed',
  failed: 'failed'
}

export { taskStatus, testStatus }
