{% extends "layouts/page.njk" %}
{% from "./components/dependency-row/macro.njk" import dependencyRow %}

{% block content %}
  {{ appPageHeading({
    text: entity,
    intro: 'Dependencies'
  }) }}

  {% call appFilters({
    action: "",
    clear: {
      url: ""
    }
  }) %}


    <div class="app-filters__item">
      {{ appAutocomplete({
        id: "entityVersion",
        name: "entityVersion",
        classes: "app-autocomplete--tiny app-autocomplete--inline",
        label: {
          text: "Service version",
          classes: "govuk-label--s app-label--inline govuk-!-margin-bottom-1"
        },
        icon: appSearchIcon(),
        formGroup: {
          classes: "app-form-group--inline"
        },
        value: formValues.entityVersion,
        suggestions: dependenceNameFilters,
        typeahead: true,
        placeholder: "Version"
      }) }}
    </div>


    <div class="app-filters__item">
      {{ appAutocomplete({
      id: "filters-team",
      name: "dependnecyType",
      classes: "app-autocomplete--tiny app-autocomplete--inline",
      label: {
        text: "Dependnecy type",
        classes: "govuk-label--s app-label--inline govuk-!-margin-bottom-1"
      },
      formGroup: {
        classes: "app-form-group--inline"
      },
      value: formValues.dependnecyType,
      suggestions: teamFilters
      }) }}
    </div>

    <div class="app-filters__item">
      {{ appAutocomplete({
        id: "filters-test-suite",
        name: "dependencyVersion",
        classes: "app-autocomplete--tiny app-autocomplete--inline",
        label: {
          text: "Dependency version",
          classes: "govuk-label--s app-label--inline govuk-!-margin-bottom-1"
        },
        icon: appSearchIcon(),
        formGroup: {
          classes: "app-form-group--inline"
        },
        value: formValues.dependnecyVersion,
        suggestions: dependenceNameFilters,
        typeahead: true,
        placeholder: "Version"
      }) }}
    </div>

  {% endcall %}

  {% block xhrContent %}
    <div data-xhr="{{ xhrElement }}">
      {% call(params) appEntityTable(tableData) %}
        {{  dependencyRow({
          rowClasses: params.rowClasses,
          loopIndex: params.loopIndex,
          entity: entity,
          entityVersion: params.row.entityVersion,
          entityTags: params.row.entityTags,
          dependency:  params.row.dependency,
          dependencyVersion: params.row.dependencyVersion,
          dependencyType: params.row.dependencyType
        }) }}
      {% endcall %}
    </div>
  {% endblock %}

{% endblock %}
