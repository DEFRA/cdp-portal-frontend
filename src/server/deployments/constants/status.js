const deploymentStatus = {
  running: 'running',
  stopping: 'stopping',
  pending: 'pending',
  requested: 'requested',
  stopped: 'stopped',
  failed: 'failed',
  undeployed: 'undeployed'
}

export { deploymentStatus }
